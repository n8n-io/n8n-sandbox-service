name: SDK Publish

on:
  pull_request:
    types: [closed]
    branches:
      - 'sdk/release/**'

permissions:
  # Required to mint an OIDC token for npm Trusted Publishing and provenance.
  id-token: write
  # The tag, release and post-release PR are created with the release app's
  # token, so GITHUB_TOKEN only needs to read the checkout.
  contents: read

jobs:
  publish:
    environment: npm
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk
    steps:
      # Without persist-credentials: false the checkout leaves GITHUB_TOKEN in
      # git's extraheader, which wins over the token in the remote URL set
      # below, so the tag would be pushed by github-actions[bot] instead of the
      # release app.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.event.pull_request.base.ref }}
          fetch-depth: 100
          persist-credentials: false

      # Node 24 ships with npm >=11.5.1, which is required for npm Trusted
      # Publishing to exchange the OIDC token for a registry auth token. The
      # SDK's minimum supported runtime is set by `engines` and the tsup build
      # target; the Node version used to pack and publish doesn't affect that.
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "24.14.1"
          registry-url: "https://registry.npmjs.org"

      - uses: pnpm/action-setup@a0ea98b2dc7d387a59324835f7421c1d5f8357b4 # v6
        with:
          package_json_file: sdk/package.json

      - name: Read version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Dry-run publishing
        env:
          RELEASE_BRANCH: ${{ github.event.pull_request.base.ref }}
        run: pnpm publish --dry-run --publish-branch "$RELEASE_BRANCH"

      - name: Publish to npm
        env:
          RELEASE_BRANCH: ${{ github.event.pull_request.base.ref }}
        run: pnpm publish --publish-branch "$RELEASE_BRANCH"

      # The sdk/v* tag ruleset only lets the release app create tags.
      - name: Generate GitHub App Token
        id: generate-token
        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf # v2.2.1
        with:
          app-id: ${{ secrets.N8N_ASSISTANT_APP_ID }}
          private-key: ${{ secrets.N8N_ASSISTANT_PRIVATE_KEY }}

      - name: Create git tag
        working-directory: .
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          TAG="sdk/v${VERSION}"
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
          # Not forced: a released tag must keep pointing at the commit its package
          # was published from, and the ruleset rejects moving it anyway.
          git tag "${TAG}"
          git push origin "${TAG}"

      - name: Create GitHub Release
        working-directory: .
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "sdk/v${VERSION}" \
            --title "@n8n/sandbox-client v${VERSION}" \
            --notes ""

      - name: Open version-bump PR against main
        working-directory: .
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git fetch origin main
          git checkout --detach origin/main
          cd sdk
          npm version "${VERSION}" --no-git-tag-version --allow-same-version
          pnpm install --lockfile-only
          cd ..

          npm ci --prefix .github/scripts
          node .github/scripts/create-signed-commit-pr.mjs \
            --base main \
            --branch "sdk/post-release/${VERSION}" \
            --title "sdk: bump version to v${VERSION}" \
            --body "Automated post-release PR to update version on main after publishing @n8n/sandbox-client@${VERSION}." \
            --label "release/sdk" \
            --message "sdk: bump version to v${VERSION}" \
            -- sdk/package.json sdk/pnpm-lock.yaml
